import { IconRocket, IconClick } from "@tabler/icons-react";
import { Summary } from "@/components/summary";

# Alternative hooks

This section contains information about alternative hooks that are built to
facilitate different querying methods, such as firing `POST` requests
immediately or firing `GET` requests on demand.

<Summary
  basePath="alternative-hooks"
  anchorPath={[
    { title: "Retrieval create", href: "retrieval-create", icon: IconRocket },
    {
      title: "Creational retrieve",
      href: "creational-retrieve",
      icon: IconClick,
    },
  ]}
/>
